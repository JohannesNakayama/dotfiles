#!/usr/bin/env bash

# ------------------------------------------------------------------------------
# --- Quick access to todo lists -----------------------------------------------
# ------------------------------------------------------------------------------

todo_dir="$HOME/Sync/notes/todo"
file=$(ls "$todo_dir" | rofi -dmenu -i -p "Select a file")
if [ -n "$file" ]; then
  $EDITOR "$todo_dir/$file"
fi
